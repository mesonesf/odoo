<odoo>
    <record id="view_purchase_order_approval_form" model="ir.ui.view">
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//header" position="inside">
                <button name="button_approve_wizard" string="Firmar Aprobación" type="object" class="btn-success" invisible="not is_current_user_approver or approval_status != 'to_approve'"/>
                <button name="button_reject_wizard" string="Rechazar" type="object" class="btn-danger" invisible="not is_current_user_approver or approval_status != 'to_approve'"/>
            </xpath>
            <xpath expr="//field[@name='partner_id']" position="after">
                <group colspan="4" col="4" invisible="approval_status == 'draft'">
                    <field name="approval_status" widget="badge" decoration-success="approval_status == 'approved'" decoration-warning="approval_status == 'to_approve'"/>
                    <field name="is_current_user_approver" invisible="1"/>
                    <field name="approver_ids" widget="many2many_tags" readonly="1"/>
                    <field name="approved_user_ids" widget="many2many_tags" readonly="1"/>
                </group>
            </xpath>
        </field>
    </record>
</odoo>