<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="view_medical_clinical_record_list" model="ir.ui.view">
            <field name="name">medical.clinical.record.list</field>
            <field name="model">medical.clinical.record</field>
            <field name="arch" type="xml">
                <list>
                    <field name="name"/>
                    <field name="patient_id"/>
                    <field name="specialty_id"/>
                    <field name="date"/>
                    <field name="practitioner_id"/>
                    <field name="state"/>
                </list>
            </field>
        </record>

        <record id="view_medical_clinical_record_form" model="ir.ui.view">
            <field name="name">medical.clinical.record.form</field>
            <field name="model">medical.clinical.record</field>
            <field name="arch" type="xml">
                <form>
                    <header>
                        <button name="action_complete" type="object" string="Completar Ficha" 
                                class="btn-primary" invisible="state != 'draft'"/>
                        <button name="action_view_evaluation" type="object" string="Ver Evaluación" 
                                class="btn-secondary" invisible="not evaluation_id"/>
                        <field name="state" widget="statusbar" statusbar_visible="draft,completed"/>
                    </header>
                    <sheet>
                        <group>
                            <group>
                                <field name="name" readonly="1"/>
                                <field name="patient_id" required="1"/>
                                <field name="specialty_id" required="1"/>
                            </group>
                            <group>
                                <field name="date" required="1"/>
                                <field name="practitioner_id" required="1"/>
                                <field name="template_id" required="1"/>
                            </group>
                        </group>

                        <notebook>
                            <page string="Cuestionario" invisible="not template_id">
                                <field name="answer_ids" mode="list,form" nolabel="1" create="0">
                                    <list editable="bottom">
                                        <field name="question_id" readonly="1" force_save="1"/>
                                        <field name="question_type" invisible="1"/> 
                                        <field name="boolean_answer" invisible="question_type != 'boolean'" string="Respuesta"/>
                                        <field name="text_answer" invisible="question_type != 'text'" string="Respuesta"/>
                                    </list>
                                    <form>
                                        <group>
                                            <field name="question_id" readonly="1"/>
                                            <field name="question_type" invisible="1"/>
                                            <field name="boolean_answer" invisible="question_type != 'boolean'" string="Respuesta Sí/No"/>
                                            <field name="text_answer" invisible="question_type != 'text'" nolabel="1" placeholder="Ingrese la respuesta..."/>
                                        </group>
                                    </form>
                                </field>
                            </page>
                            <page string="Imagen Adjunta">
                                <group>
                                    <field name="attachment_image" filename="attachment_filename" string="Imagen"/>
                                    <field name="attachment_filename" invisible="1"/>
                                </group>
                            </page>
                        </notebook>
                    </sheet>
                </form>
            </field>
        </record>

        <record id="action_medical_clinical_record" model="ir.actions.act_window">
            <field name="name">Fichas Clínicas</field>
            <field name="res_model">medical.clinical.record</field>
            <field name="view_mode">list,form</field>
            <field name="view_id" ref="view_medical_clinical_record_list"/>
        </record>
    </data>
</odoo>