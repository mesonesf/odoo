<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_invoice_tree_inherit_payment" model="ir.ui.view">
        <field name="name">account.invoice.tree.inherit.payment</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_invoice_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='name']" position="after">
                <field name="payment_journal_names" optional="show"/>
            </xpath>
        </field>
    </record>

    <record id="view_move_form_inherit_payment" model="ir.ui.view">
        <field name="name">account.move.form.inherit.payment</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='payment_reference']" position="after">
                <field name="payment_journal_names" invisible="payment_state not in ('paid', 'in_payment', 'partial')"/>
            </xpath>
        </field>
    </record>
</odoo>